pipeline { 
    agent any  
    tools {
        jdk 'openjdk8'
        maven 'Maven'

    stages { 
        stage('Build') { 
            steps { 
               echo 'This is a minimal pipeline.' 
            }
        }
    }
}